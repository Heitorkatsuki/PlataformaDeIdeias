{{#if flash.error}}
  <div class="flash flash-error">
    {{#each flash.error}}<p>{{this}}</p>{{/each}}
  </div>
{{/if}}
{{#if flash.success}}
  <div class="flash flash-success">
    {{#each flash.success}}<p>{{this}}</p>{{/each}}
  </div>
{{/if}}